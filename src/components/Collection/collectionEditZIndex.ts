/** Modal/drawer shell for collection edit. */
export const COLLECTION_EDIT_OVERLAY_Z_INDEX = 2000;

/** Portaled comboboxes (e.g. finish Select) inside the edit overlay. */
export const COLLECTION_EDIT_DROPDOWN_Z_INDEX = 2100;

/** Portaled tooltips inside the edit overlay (must be above overlay + dropdowns). */
export const COLLECTION_EDIT_TOOLTIP_Z_INDEX = 2200;
