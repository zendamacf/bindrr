export function scryfallCardUrl(scryfallId: string): string {
  return `https://scryfall.com/card/${scryfallId}`;
}

export function tcgplayerProductUrl(tcgplayerProductId: string): string {
  return `https://www.tcgplayer.com/product/${tcgplayerProductId}`;
}
