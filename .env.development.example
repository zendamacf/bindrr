# Local development (npm run start:dev / db:migrate / start)
# Copy to .env in the repo root: cp .env.development.example .env
DATABASE_URL=postgresql://bindrr:bindrr@localhost:5432/bindrr
AUTH_SECRET=
OPENEXCHANGERATES_APPID=
CRON_SECRET=
PUBLIC_SENTRY_DSN=
PORT=3000
