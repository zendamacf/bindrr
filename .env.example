# Production Docker Compose (.env next to docker-compose.yml on the VPS)
DB_PASSWORD=
AUTH_SECRET=
OPENEXCHANGERATES_APPID=
CRON_SECRET=
PUBLIC_SENTRY_DSN=
APP_PORT=3000
# Published image from a v* tag (see .github/workflows/publish-docker.yml)
APP_IMAGE=ghcr.io/zendamacf/bindrr:latest
